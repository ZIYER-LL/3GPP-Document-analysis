services:
  db:
    image: postgres:16
    container_name: 3gpp_tdoc_db
    environment:
      POSTGRES_DB: 3gpp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: